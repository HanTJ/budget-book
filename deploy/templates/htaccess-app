# ==============================================================
# Budget Book — public_html/app/.htaccess
# 이 디렉터리 이하(vendor, src, config, database)는 외부에서 직접
# 접근할 수 없어야 한다. Apache 레벨에서 모두 403 처리한다.
# ==============================================================
Require all denied
